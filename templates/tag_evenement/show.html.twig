{% extends 'base.html.twig' %}

{% block title %}TagEvenement{% endblock %}

{% block body %}
    <h1>TagEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag_evenement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tag_evenement.nom }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ tag_evenement.couleur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tag_evenement_index') }}">back to list</a>

    <a href="{{ path('app_tag_evenement_edit', {'id': tag_evenement.id}) }}">edit</a>

    {{ include('tag_evenement/_delete_form.html.twig') }}
{% endblock %}
